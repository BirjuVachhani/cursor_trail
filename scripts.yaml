clean:
  - flutter clean && flutter pub get
  - cd example && flutter clean && flutter pub get
format:
  - dart format --fix .
build:
  web: cd example && flutter build web
  macos: cd example && flutter build macos